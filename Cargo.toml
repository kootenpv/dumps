[package]
name = "dumps"
version = "0.1.0"
edition = "2021"

[lib]
name = "dumps"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.23", features = ["extension-module"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
base64 = "0.22"

[profile.release]
lto = true